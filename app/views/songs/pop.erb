<h1>Pop Songs</h1>

<ul>

<% @songs.each do |song| %>
    <% if song.genre == "Pop" %>
        <a href="/songs/<%=song.id%>">"<%= song.name %>" - <%= song.artist %></a><br>
    <% end %>
<% end %>

</ul>